//TODO: write details of the function

pub fn tcs(
    input: &str,
    param: &str,
    keep_original: bool,
) -> Result<(), Box<dyn std::error::Error>> {
    todo!(
        "TCS function called with input: {}, param: {}, keep_original: {}",
        input,
        param,
        keep_original
    );
}

pub fn tcs_dr(
    input: &str,
    version: Option<String>,
    keep_original: bool,
) -> Result<(), Box<dyn std::error::Error>> {
    todo!(
        "TCS DR function called with input: {}, version: {:?}, keep_original: {}",
        input,
        version,
        keep_original
    );
}
